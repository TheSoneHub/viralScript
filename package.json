{
  "name": "viralscript",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "dev": "netlify dev",
    "start": "netlify dev",
    "build": "echo 'No build step'",
    "deploy": "netlify deploy --prod --dir=."
  },
  "devDependencies": {
    "netlify-cli": "^9.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
